/**
 * Created by Samer on 2015-08-10.
 */
/*
var MongoClient = require('mongodb').MongoClient, assert = require('assert'), ObjectId = require('mongodb').ObjectID;

// Connection URL
var url = 'mongodb://localhost:27017/starForums';
// Use connect method to connect to the Server
var dbCon;
MongoClient.connect(url, function (err, dbConnection) {
    assert.equal(null, err);
    dbCon = dbConnection;
    console.log("Connected correctly to server. Server can now send queries.");
});
    */